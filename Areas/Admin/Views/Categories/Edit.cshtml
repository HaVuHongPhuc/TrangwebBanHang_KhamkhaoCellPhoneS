@model TrangwebCellPhoneS.Models.Category

@{
    ViewBag.Title = "Cập nhật Danh mục";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<style>
    /* Dùng chung style với Create nhưng đổi màu viền */
    .admin-form-card {
        background: #fff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 0 15px rgba(0,0,0,0.05);
        max-width: 600px;
        margin: 30px auto;
        border-top: 4px solid #ffc107; /* Màu vàng cho Edit */
    }

    .form-title {
        font-size: 22px;
        font-weight: bold;
        color: #333;
        margin-bottom: 20px;
        text-align: center;
        text-transform: uppercase;
    }
</style>

<div class="admin-form-card">
    <h2 class="form-title">Cập nhật Danh Mục</h2>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @Html.HiddenFor(model => model.CategoryID)

        @* Gọi Partial View Form *@
        @Html.Partial("PartialView_CreateEditCategory", Model)
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}